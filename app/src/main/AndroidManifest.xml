<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saproject3">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SAProject3">
        <activity android:name=".BookDetails2"></activity>
        <activity
            android:name=".BookingDetails"
            android:label="@string/title_activity_booking_details" />
        <activity
            android:name=".MainHomeActivity"
            android:label="@string/title_activity_main_home"
            android:theme="@style/Theme.SAProject3.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>